# Cloudflare Pages Redirects
# This file handles URL normalization and redirects
# Format: <source> <destination> <status_code>

# Force HTTPS
http://www.deadlyblox.com/* https://www.deadlyblox.com/:splat 301!

# Trailing slash normalization - redirect all URLs without trailing slash to versions with trailing slash
# This prevents duplicate content issues and matches Next.js trailingSlash: true config
# Note: This works with next.config.js trailingSlash: true to ensure consistent URL structure

# If you have any old URLs that need to redirect to new ones, add them below:
# /old-page /new-page 301
